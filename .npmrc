# engine-strict is deliberately NOT enabled. The `engines` field in package.json documents
# the requirement and pnpm warns on mismatch, but hard-failing turns every nested script
# invocation (Playwright's webServer, run-s steps) into an opaque install error rather than
# a readable warning. CI pins the Node version, which is where the guarantee belongs.
auto-install-peers=true
